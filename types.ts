export interface Specialty {
  title: string;
  text: string;
}

export interface TestimonialData {
  text: string;
  name: string;
  before: string;
  after: string;
}

export interface NavLink {
  href: string;
  label: string;
}